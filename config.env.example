# Server Configuration
NODE_ENV=development
# Application environment mode (production, development)
# Controls logging level, error handling, and performance optimizations

PORT=8000
# Port number where the backend server listens for incoming requests

JWT_COOKIE_EXPIRES_IN=1
# JWT authentication token expiration time (in days)
# How long users stay logged in before needing to re-authenticate

# Email Configuration
From_Email=
# Sender email address for outgoing emails (e.g., noreply@app.com)
# Used in password reset, welcome emails, and notifications

HOST_URL=
# Frontend/application base URL (e.g., https://series-locker.netlify.app)
# Used in email links and CORS configuration

# Database Configuration
DATABASE=
# MongoDB Atlas connection string (cloud database)
# Format: mongodb+srv://username:<password>@cluster.mongodb.net/dbname?options
# Replace <PASSWORD> with DATABASE_PASSWORD value

DATABASE_PASSWORD=
# MongoDB password for DATABASE connection string
# Application replaces <PASSWORD> placeholder with this value

DATABASE_LOCAL=mongodb://localhost:27017/series-locker
# Local MongoDB connection string (development only)
# Used when running MongoDB locally without authentication

# Security Configuration
SECRET_KEY=
# JWT secret key for signing authentication tokens
# Must be long and random; never share or commit

SECRET_EXPIRES_IN=1d
# JWT token expiration duration (e.g., 1d = 1 day, 7d = 7 days, 24h = 24 hours)
# How long JWT tokens remain valid before requiring re-login

# Email Service Configuration
SENDGRID_USERNAME=
# SendGrid API username or sender email for SMTP authentication
# Required for email delivery (password reset, notifications)

SENDGRID_PASSWORD=
# SendGrid API key/password for SMTP authentication
# Generate from SendGrid dashboard; treat as sensitive secret
